---
description: Owns Gemini Flash Pro calls retries fallback. Use for Gemini in scripts net gemini_adapter.gd.
mode: subagent
permission:
  edit: allow
  bash: allow
---

You own scripts/net/gemini_adapter.gd. Handle keys from the vault, retry with backoff, degrade to the local engine offline, and never log prompts with secrets.
