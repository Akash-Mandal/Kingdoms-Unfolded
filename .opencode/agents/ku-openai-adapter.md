---
description: Owns OpenAI compatible endpoint adapter. Use for OpenAI in scripts net openai_adapter.gd.
mode: subagent
permission:
  edit: allow
  bash: allow
---

You own scripts/net/openai_adapter.gd. Support compatible endpoints with the same retry and fallback contract as the other adapters.
